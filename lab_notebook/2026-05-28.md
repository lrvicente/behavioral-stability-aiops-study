# Lab Notebook — 2026-05-28

## Context

Cycle 1 in execution (T+13 days). Pre-pilot window of the GPU node (27–29 May).
This entry documents decisions taken today that motivate pre-registration
amendment v1.1.

## Decision 1 — GPU node (cnsm-gpu-01) brought up during pre-pilot

The protocol (EXPERIMENTAL_SETUP.md §3.2.1) places the Lambda A10 as a monitored
host in Sprint S2 (weeks 9–12). Today, in the pre-pilot window, the A10 was
provisioned, instrumented (Zabbix agent, 15 GPU metrics, ground-truth
UserParameters), given a baseline workload, and a GPU fault injector with 5
classes. See protocols/GPU_NODE_SETUP.md for full detail.

Framing: this is TECHNICAL VALIDATION, not study data collection. Data produced
during the pre-pilot (28–29 May) is for pipeline validation and will NOT enter
the cycle-1 dataset used for training/evaluation. The S2 collection (weeks 9–12)
remains the pre-registered data-collection window. This preserves the zero-shot
cross-environment design (the model must not see GPU data during training).

## Decision 2 — Divergences to reconcile before S2

The pre-pilot tooling diverges from the LOCKED protocol §5.2 and must be
reconciled (or formally amended) BEFORE S2 collection begins:

- Fault class names/mechanisms differ from the pre-registered ontology mapping
  (§5.2.1). Pre-pilot used: gpu_compute_stress, vram_pressure, pcie_bandwidth_sat,
  network_degradation, cuda_process_kill. Protocol specifies: gpu_utilization_spike,
  gpu_memory_pressure, disk_io_contention, data_loader_bottleneck,
  batch_inference_overload (+ cpu_contention_during_training). RQ2/RQ3 depend on
  the ontology mapping, so the S2 classes must follow §5.2.1.
- Workload: pre-pilot used ResNet50 INFERENCE at 50–85% utilization. Protocol
  §4.2 specifies ResNet50 TRAINING loop at 80–95% + batch inference every 30 min.
- Schedule: pre-pilot used pre_pilot_schedule.csv (seed 142, 10 events). Protocol
  §5.2 specifies fault_schedule_lambda_cycle1.csv (seed 42, 28 days, ~200 events).

Action: before S2, align the GPU fault classes to the §5.2.1 ontology mapping,
switch workload to training profile, and generate the pre-registered lambda
schedule. The pre-pilot tooling (infra, monitoring, injection mechanism) is
reusable; the parameters must be brought into protocol compliance.

## Decision 3 — Remove Essex from program documents

PhD strategy pivoted to ISCTE-IUL (Lisbon), a traditional 4-year doctoral
program, away from the Essex PhD-by-Publication route (abandoned). Today's
amendment removes Essex references from OPERATIONAL_GUIDE.md and
THREE_CYCLE_PLAN.md, using neutral "doctoral portfolio/writing" wording. The
"PhD by Publication" framing itself (distinct from the traditional ISCTE model)
is left for a later, separate revision once the ISCTE framing is finalized.

## Amendment

These changes are recorded under tag pre-registration-cycle1-v1.1. The LOCKED
artifacts changed in v1.1: OPERATIONAL_GUIDE.md, THREE_CYCLE_PLAN.md (Essex
removal). The GPU divergences (Decision 2) are documented here as pending
reconciliation; the protocol §5.2 is NOT yet rewritten, pending the decision on
whether to align to it or formally amend it before S2.

## Decision 4 — Compress inter-cycle gaps to 7 days (Gantt v1.2)

The original THREE_CYCLE_PLAN spaced cycles approximately 47 and 60 days apart
(cycle 1 ends 13/08, cycle 2 starts 01/10; cycle 2 ends 29/12, cycle 3 starts
01/03). These gaps were intended for paper drafting, analysis, and cycle setup
between research questions.

Decision: compress both gaps to 7 days each. Rationale:
- Better use of OVH voucher (expires 2026-05-07): finishing cycle 3 collection
  on 23/02/2027 leaves 73 days for ML training, paper finalization, and
  unforeseen issues, instead of 0–10 days with the original schedule.
- Earlier paper submissions and earlier completion of the doctoral portfolio.
- Pre-pilot has already validated infra and tooling end-to-end (Decision 1),
  reducing the risk of needing the gaps for "lessons incorporation".

Acknowledged risks (accepted):
- Paper drafting for cycle N is concurrent with cycle N+1 collection. This is
  high-load for a solo researcher, mitigated by automated nightly export and
  rolling analysis (analysis scripts run weekly, not only at cycle end).
- If cycle N has a data quality issue, it may not be discovered before cycle
  N+1 starts. Mitigation: rolling analysis (described above) and weekly check
  of dataset integrity in the lab notebook.

New cycle dates (Gantt v1.2):
- Cycle 1: 2026-05-15 to 2026-08-13 (unchanged)
- Cycle 2: 2026-08-21 to 2026-11-18 (was 2026-10-01)
- Cycle 3: 2026-11-26 to 2027-02-23 (was 2027-03-01)
- ML training + finalization buffer: 2027-02-24 to 2027-05-07 (73 days)

Pre-registration commit deadlines shift accordingly:
- fault_schedule_cycle2.csv: already committed under tag pre-registration-cycle1-v1.0
  on 2026-05-12, so the artifact predates the new cycle 2 start (2026-08-21). No
  re-commit needed; the LOCKED tag covers it.
- fault_schedule_cycle3.csv: same — already committed in v1.0.
- THREE_CYCLE_PLAN.md "Pre-registration commits" section will be updated in this
  amendment to reflect the new dates.
