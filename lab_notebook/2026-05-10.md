# 2026-05-10

## Milestone: Zabbix nightly export to OVH Object Storage validated

The Zabbix export pipeline was created and tested successfully.

Validated path:

Zabbix API → local export → JSONL/Parquet → OVH Object Storage

Exported data:
- 12 monitored study hosts
- 756 enabled monitored items
- 22,851 history rows in the first manual test

Object Storage destination:
`s3://cnsm2027-study/raw/zabbix/daily/2026/05/10/`

Generated files:
- `history.jsonl.gz`
- `history.parquet`
- `hosts.json`
- `items.json`
- `manifest.json`
- `summary.csv`

The Object Storage bucket is now the canonical preservation layer for the study dataset.

## Milestone: Workloads active and heartbeat validated

All study workloads were deployed via Ansible using playbook `20-workloads.yml`.

The service `cnsm-workload` is active across the 12 study hosts.

A heartbeat sidecar was deployed to update `/var/run/cnsm-workload-heartbeat`.

Zabbix item `cnsm.workload.heartbeat` is now reporting `1`, confirming workload activity.

Fault injection has not been activated yet. The environment is now entering baseline observation mode.

### Validation evidence

Validated through:

```bash
ansible-playbook -i inventory/hosts.ini playbooks/20-workloads.yml
ansible all -i inventory/hosts.ini -b -m command -a "systemctl is-active cnsm-workload"
ansible all -i inventory/hosts.ini -b -m shell -a "zabbix_agent2 -t cnsm.workload.heartbeat"
```

Observed result:

- `cnsm-workload` active on all 12 study hosts.
- `cnsm.workload.heartbeat` reporting `1` in Zabbix.
- Workload telemetry visible in Zabbix graphs.
- Fault injection remains disabled.
