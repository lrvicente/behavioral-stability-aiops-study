---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart cnsm-workload
  ansible.builtin.systemd:
    name: cnsm-workload
    state: restarted

- name: Restart redis
  ansible.builtin.systemd:
    name: redis-server
    state: restarted
